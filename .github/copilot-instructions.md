- [x] Verify that the copilot-instructions.md file in the .github directory is created.

- [x] Clarify Project Requirements
	React 18 + TypeScript + Vite PWA for a custom Mealie client targeting iOS standalone install behavior, Docker deployment, and a warm editorial design system.

- [x] Scaffold the Project
	Created the Vite + React + TypeScript + Tailwind + PWA project structure directly in the current workspace root.

- [x] Customize the Project
	Implemented the Mealie API client, settings storage, recipes, meal plan, shopping list, recipe detail view, PWA shell, and editorial design system.

- [x] Install Required Extensions
	No extensions were required by the Vite project setup information.

- [ ] Compile the Project
	Blocked: the current machine does not have Node.js or npm installed, so dependencies cannot be installed and the production build cannot be executed yet.

- [ ] Create and Run Task
	`.vscode/tasks.json` was created with a `Run Mealie PWA Dev Server` task. Execution is currently blocked because npm is not installed.

- [ ] Launch the Project
	Deferred until Node.js and npm are installed and the user wants to launch or debug the app.

- [x] Ensure Documentation is Complete
	README.md has been updated for beginners, and this file has been cleaned up to remove the setup comments.

- Work through each checklist item systematically.
- Keep communication concise and focused.
- Follow development best practices.